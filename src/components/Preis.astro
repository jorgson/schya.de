---
interface Props {
  title: string;
  minutes: string;
  price: string;
}

const { title, minutes, price } = Astro.props;
---

<div class="flex flex-col items-center">
  <h3 class="text-2xl font-bold gap-8">
    <span class="leading-6"
      >{title} <span class="text-sm"><br />{minutes} Minuten</span></span
    >
    <span
      class="border block mx-auto mt-2 border-dark-grey rounded-full px-4 w-[100px]"
      >{price} €</span
    >
  </h3>
</div>
